<Page
    x:Class="RADIANT_SPARK.Story"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RADIANT_SPARK"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid KeyDown="Grid_KeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Image Source="Assets/story.jpg" Stretch="UniformToFill" Grid.ColumnSpan="3" Grid.RowSpan="3"
               HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBlock x:Name="myTextBlock" Text="STORY" FontFamily="{StaticResource NHL_Ducks}" 
            FontSize="150" Foreground="AliceBlue"  HorizontalAlignment="Center" Grid.Column="1"/>
        <TextBlock x:Name="myText" TextWrapping="Wrap" FontFamily="{StaticResource Aboreto}" FontWeight="Bold"
            FontSize="35" Foreground="AliceBlue"  HorizontalAlignment="Center" Grid.Row="1" Grid.Column="1" TextAlignment="Center">
            In a distant future, five wise governors try to solve the society's population problems by suggesting the use of technological progress.
            five great cores are created, distinguished by their job.
            the divisions made between the citizens generate instability, so a group of conservative inhabitants rise up and create a whole new community based on a traditionalist and religious ideology.
            this will start a violent confrontation between the two opposed  beliefs: ''the old world'' and ''the new world''.
        </TextBlock>
        <TextBlock x:Name="Continue" Text="PRESS ANY KEY TO CONTINUE" FontFamily="{StaticResource NHL_Ducks}"
            FontSize="90" Foreground="AliceBlue"  HorizontalAlignment="Center" Grid.Column="1" Grid.Row="2" Visibility="Collapsed"/>
        <ScrollViewer IsTabStop="True" Width="10000" Height="1000" Grid.RowSpan="3" Grid.ColumnSpan="3" FocusVisualMargin="-6"/>
    </Grid>
</Page>
