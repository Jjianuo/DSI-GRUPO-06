<Page
    x:Class="RADIANT_SPARK.LoadingScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RADIANT_SPARK"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Image Source="Assets/loading_screen.png" Stretch="UniformToFill" Grid.ColumnSpan="3" Grid.RowSpan="2"
               HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Viewbox VerticalAlignment="Bottom">
            <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Width="1480">
                <TextBlock x:Uid="LoadingT_LoadingS" FontFamily="{StaticResource NHL Ducks}" 
            FontSize="50" Foreground="AliceBlue" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
                <ProgressBar Margin="20, 20, 20, 20" Height="50" VerticalAlignment="Bottom" HorizontalAlignment="Center" Width="1400"
                         IsIndeterminate="False" Value="0" Foreground="SteelBlue" x:Name="myProgressBar"/>
            </StackPanel>
        </Viewbox>
    </Grid>
</Page>
